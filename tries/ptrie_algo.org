

** Вставить
 берем корень, смотрим первый бит в dst адресе, по нему мы понимаем куда нам идти дальше(лево или право)
 Заходим в следующую ноду, внутри нее должен быть офсет и биты для следующего поиска, если ((dst_addr << offset) & offset_bites), то мы считаем, что можем ходить дальше, тут нужно понять что хранить в ноде:
 оффсет до предыдущей ноды, или до коря.
 Когда мы понимаем, что следующей ноды нет, или & прошел не удачно, мы должны создать новую ноду. Для этого сначала мы должны понять, на каком бите нужно сделать ветку:
 например у нас есть адрес 11101100111, в ноде лежит 11101110111, не совпадение в бите 7, нам нужно сделать новые ноды:
 111011
       00111
       10111

 Тоесть, условно у нас было дерево c двумя префиксами
                                  root
				0     11101100111


Теперь нам нужно создать вот такое дерево:

                                  root
		              0         111011
				     00111    10111

Если в старой правой ноде от корня, мы хранили:  len =  11, offset_bytes = 11101100111
То теперь у нас есть ноды:
                        parent->right:          len = 6, offset_bites = 11101100000 в старый я должен с=записать но
			parent->right->left:    len = 11,offset_bites = 11101100111
			parent->right->right:   len = 11,offset_bites = 11101110111
Как понять, в каком байте я должен делать ветку?
у меня есть биты в parent->right и dst_address
мы может

11101100111
11101110111
если сделать между ними xor, то первый не совпадающий бит будет помечен 1

